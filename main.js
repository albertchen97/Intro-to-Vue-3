// Create a Vue app object
const app = Vue.createApp({
    // Create a data property that returns the product name
    data:
        function () {
            return {
            product: 'Socks'
        }
    }
})

